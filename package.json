{
    "name": "seiseisai-archives",
    "private": true,
    "scripts": {
        "build": "node scripts/build-pages.mjs",
        "check": "biome check",
        "format": "biome format --write _worker.js _routes.json package.json biome.json scripts/build-pages.mjs",
        "dev": "node scripts/build-pages.mjs && wrangler pages dev dist --local"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.5.11",
        "@cloudflare/workers-types": "^5.20260830.1",
        "wrangler": "^4.127.1"
    }
}
